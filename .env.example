# Server only. Never prefix this with VITE_, NEXT_PUBLIC_, or expose it in browser code.
OPENAI_API_KEY=sk-your-key-here

# Change these independently as your quality / cost needs evolve.
OPENAI_TEXT_MODEL=gpt-5.6-terra
OPENAI_IMAGE_MODEL=gpt-5.6-terra
OPENAI_IMAGE_QUALITY=medium
OPENAI_IMAGE_SIZE=1536x1024
PORT=3000
